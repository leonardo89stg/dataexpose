<?php
$dir = getenv('HOME') . '/.local/share/applications/';
$files = glob($dir . '*.desktop');

foreach ($files as $file) {
    $filename = basename($file); // Obtém apenas o nome do arquivo, sem o caminho
    echo $filename . "<br>";
}
?>
